fof-oauth:
  admin:
    settings:
      providers:
        mediawiki:
          description: Configure your MediaWiki OAuth2 application. See the documentation at {link}

          base_url_label: MediaWiki Base URL
          client_id_label: => fof-oauth.ref.settings.client_id
          client_secret_label: => fof-oauth.ref.settings.client_secret

  forum:
    log_in:
      with_mediawiki_button: '=> fof-oauth.forum.log_in.with_button'

    providers:
      mediawiki: '=> fof-oauth.lib.providers.mediawiki'

  lib:
    providers:
      mediawiki: MediaWiki
